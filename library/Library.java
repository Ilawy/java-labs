
import liblib.GUI;

// streams
// for each libItem (relation between items and clients)

// update for each (item, client)
// ~~~~~~~~~~~~~~~~ stock with `borrowLibItem(userid, itemid)` OR each book has single copy (available or not)
// `returnItem(itemid)` for each item
// create interface for (item, client)
// 


//set all ids to uuid

// class ItemContainer{
//     LibItem item;
//     int qty;
// }


public class Library {



    public static void main(String[] args) {
        GUI gui = new GUI();

    }
}
